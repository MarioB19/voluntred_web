import { Box, Typography } from '@mui/material';

export default function Involucrate() {
  return (
    <Box id="involucrate" sx={{ py: 8, bgcolor: 'primary.light' }}>
      <Typography variant="h2" color="primary.dark">Involúcrate</Typography>
      <Typography>Regístrate como Voluntario y Dona Ahora.</Typography>
    </Box>
  );
}